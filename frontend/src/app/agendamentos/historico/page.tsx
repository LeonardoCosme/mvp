import { redirect } from 'next/navigation';

export default function AgendamentosHistoricoRedirectPage() {
  redirect('/historico');
}


// frontend/src/app/agendamentos/historico/page.tsx
import { redirect } from 'next/navigation';

export default function AgendamentosHistoricoRedirectPage() {
  // Redireciona para a página de histórico que já existe
  redirect('/historico');
}